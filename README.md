# NATS + Streaming Cluster Demo

Stands up a 6 node cluster for NATS + Streaming

## Usage

Start up the cluster using the following command.

```
make start
```

Wait until the "cluster" is running, and then run benchmarks as follows.

```
make benchmark_all
```
